import React from 'react';

import { StyledSelectors } from './style';

const ColourSelectors = () => {

  return (
    <StyledSelectors>{1}</StyledSelectors>
  );
};

export default ColourSelectors;
